<div id="main2">
  <div id="mtab1" class="tab">
    <div class="tab-list">
      <div id="m1tab1">
        <div class="items">
          <ul>
            {% for item in items %}
              <li{% if loop.index % 4 == 0 %} class="nth"{% endif %}>
                <div class="icon">
                  <img src="{{ asset('bundles/mgfront/images/icon7.png') }}" width="24" height="24">
                </div>
                <div class="item-img">
                  <a href="{{ url('ns_catalog_item', { 'categorySlug': item.category.slug, 'itemSlug': item.slug}) }}">
                    <img src="{{ '/upload/j/' ~ item.settings.photo }}"
                         alt="{{ item.title }}"
                         width="243" height="156"/>
                  </a>
                </div>
                <a href="{{ url('ns_catalog_item', { 'categorySlug': item.category.slug, 'itemSlug': item.slug}) }}">{{ item.title }}</a>

                <div class="price">
                  <ul>
                    {% for price in item.settings.prices %}
                      {% if loop.index < 4 %}
                        <li>
                          <span>{{ price.weight }} г</span>
                          <strong>{{ price.price }} руб</strong>

                          <div class="drop">
                            <span>{{ price.weight }} г</span>
                            <strong>{{ price.price }} руб</strong>
                            <ul data-product-id="{{ item.id }}">
                              <li><a href="#">1 пирог</a></li>
                              <li><a href="#">2 пирога</a></li>
                              <li><a href="#">3 пирога</a></li>
                              <li><a href="#">4 пирога</a></li>
                              <li><a href="#">5 пирогов</a></li>
                            </ul>
                          </div>
                        </li>
                      {% endif %}
                    {% endfor %}
                  </ul>
                </div>
              </li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
  </div>
  {#<div class="more">#}
  {#<a href="#">Показать еще</a>#}
  {#</div>#}
</div>

{% javascripts
'@MGFrontBundle/Resources/public/js/app/ProductModel.js'
'@MGFrontBundle/Resources/public/js/app/CatalogView.js' %}
<script src="{{ asset_url }}"></script>
{% endjavascripts %}

<script type="text/javascript">
  $(function () {
//    var products = new MGApp.ProductCollection();

    {#{% for item in items %}#}
    {#products.add({#}

    {#});#}
    {#{% endfor %}#}

//    new MGApp.CatalogView({
//      el: $('#main2'),
//      collection: products
//    });
//  });
</script>
